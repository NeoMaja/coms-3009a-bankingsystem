<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coms_3009a_banking_system">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coms3009abankingsystem">
        <activity android:name=".Registration.adminRegister"
            android:parentActivityName=".usertype"></activity>
        <activity android:name=".Banking" />
        <activity android:name=".client_account" />
        <activity android:name=".Registration.AdminKeyView" />
        <activity android:name=".Registration.clientRegister"
            android:parentActivityName=".usertype"/>

        <activity android:name=".usertype"
            android:parentActivityName=".login"/>
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>